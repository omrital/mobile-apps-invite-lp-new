export type InviteDetails = {
  appType: string,
  title: string,
  subtitle: string,
  description: string,
  placeImageUrl: string,
  coverImageUrl: string,
  inviteLevel: string,
  vertical: string
}
